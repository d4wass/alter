import { LoginModalDirective } from './login-modal.directive';

describe('LoginModalDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginModalDirective();
    expect(directive).toBeTruthy();
  });
});
