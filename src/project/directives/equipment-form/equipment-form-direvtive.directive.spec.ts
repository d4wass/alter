import { EquipmentFormDirevtiveDirective } from './equipment-form-direvtive.directive';

describe('EquipmentFormDirevtiveDirective', () => {
  it('should create an instance', () => {
    const directive = new EquipmentFormDirevtiveDirective();
    expect(directive).toBeTruthy();
  });
});
